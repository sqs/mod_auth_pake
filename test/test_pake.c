#include "pake.h"
#include <stdio.h>
#include <string.h>

void test_pake_init_server() {
    struct pake_info p;
    memset(&p, 0, sizeof(p));
}

void test_pake_init_client() {
    struct pake_info p;
    memset(&p, 0, sizeof(p));
}
